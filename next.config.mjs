/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "d3ddx6b2p2pevg.cloudfront.net",
      "firebasestorage.googleapis.com",
    ],
  },
};

export default nextConfig;
